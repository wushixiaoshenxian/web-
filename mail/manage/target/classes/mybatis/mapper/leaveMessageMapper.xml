<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.manage.mapper.ManageMapper">
    <!--Information queryByIdList(int id);-->
    <select id="queryByIdList" resultType="LeaveMessage">
        select * from t_leavemessage where id = #{id}
    </select>

</mapper>